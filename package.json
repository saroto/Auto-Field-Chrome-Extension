{
  "name": "autofill-form-extension",
  "version": "1.0.0",
  "description": "",
  "main": "popup.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "tsc --watch",
    "build": "tsc && npm run bundle",
    "bundle": "esbuild src/content/index.js --bundle --format=iife --outfile=src/content/index.bundled.js && esbuild src/popup/index.js --bundle --format=iife --outfile=src/popup/index.bundled.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@types/chrome": "^0.1.37",
    "esbuild": "^0.27.3",
    "typescript": "^5.9.3"
  }
}
